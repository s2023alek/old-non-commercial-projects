// Project SimpleBitmapDataEngine
package org.jinanoimateydragoncat.works.sbde {
	/*
	 * SimpleBitmapDataEngine
	 * dependencies:
	 */
}

//{ =^_^= History
/* > (timestamp) [ ("+" (added) ) || ("-" (removed) ) || ("*" (modified) )] (text)
 * > 
 */
//} =^_^= END OF History

// template last modified:03.05.2010_[22#42#27]_[1]